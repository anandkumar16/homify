/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            "lh3.googleusercontent",
            "res.cloudinary.com"
        ]
    },
};

export default nextConfig;
